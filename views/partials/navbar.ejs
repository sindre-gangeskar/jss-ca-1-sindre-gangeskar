<nav id="navbar" class="navbar navbar-expand-lg navbar-light bg-light position-fixed w-100 fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/memes-overview/"><img src="/company-logo/logo.png" alt="logo" class="logo"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav">

                <% if(!user)  { %>
                    <li class="nav-item"><strong><p class="nav-link">Guest</p></strong></li>
                <% } else { %>
                    <li class="nav-item"><strong><p class="nav-link"><%- user.username %></p></strong></li>
                <% } %>

                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/memes-overview">Memes</a>
                </li>
                <% if(!user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Sign In</a>
                    </li>
                    <% } else { %>
                        <li class="nav-item">
                            <form action="/login/logout" method="post" id="logout-form">
                                <button type="submit" class="btn nav-link">Sign Out</button>
                            </form>
                        </li>
                    <% }; %>
            </ul>
        </div>
    </div>
</nav>